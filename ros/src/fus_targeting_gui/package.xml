<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematron="http://download.ros.org/schema/package_format3.xsd"?>
<package format="3">
  <name>fus_targeting_gui</name>
  <version>0.1.0</version>
  <description>
    Standalone GUI for transcranial focused ultrasound (tFUS) acoustic
    characterization. Loads a skull mesh, click a point on its surface,
    and send the robot's end effector there via MoveIt. Contains no
    robot-specific logic. Every robot-facing detail (planning group,
    base and tip frame, mesh registration pose) comes from config.yaml,
    so pointing this at a different, already-calibrated arm is a config
    change, not a code change. This is the first ament_python package in
    this workspace, since everything else is C++ and ament_cmake. It
    talks to MoveIt via pymoveit2 rather than moveit_py, which is not
    installed here.
  </description>
  <maintainer email="tylerzeng16@gmail.com">tyler zeng</maintainer>
  <license>BSD</license>

  <buildtool_depend>ament_python</buildtool_depend>

  <exec_depend>rclpy</exec_depend>
  <exec_depend>moveit_msgs</exec_depend>
  <exec_depend>geometry_msgs</exec_depend>
  <exec_depend>sensor_msgs</exec_depend>
  <exec_depend>tf_transformations</exec_depend>
  <exec_depend>ament_index_python</exec_depend>
  <!-- Not resolvable via rosdep: no released binary package. Must be
       cloned into this workspace's src/ (see README.md "Setup") so colcon
       builds it locally. Declaring it here just gets colcon's own
       intra-workspace dependency ordering right (build pymoveit2 before
       this package) when both are present in the same workspace. -->
  <exec_depend>pymoveit2</exec_depend>

  <test_depend>ament_copyright</test_depend>
  <test_depend>ament_flake8</test_depend>
  <test_depend>ament_pep257</test_depend>
  <test_depend>python3-pytest</test_depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
