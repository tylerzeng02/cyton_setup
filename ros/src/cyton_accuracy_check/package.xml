<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematron="http://download.ros.org/schema/package_format3.xsd"?>
<package format="3">
  <name>cyton_accuracy_check</name>
  <version>0.1.0</version>
  <description>
    Single combined program (run_accuracy_check) that both commands the arm
    via MoveIt and captures each pose's NDI measurement in one synchronized
    loop -- same shape as the original tests/test_five_pose_ndi_capture.cpp
    (move, settle, capture, repeat, no manual cross-terminal coordination),
    but with MoveIt doing the moving instead of raw Dynamixel ticks. Built
    after cyton_pose_commander + cyton_ndi_capture's separate two-terminal
    workflow was found to be vulnerable to a real timing race (a capture
    landing before the move had actually finished).
  </description>
  <maintainer email="tylerzeng16@gmail.com">tyler zeng</maintainer>
  <license>BSD</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <build_depend>rclcpp</build_depend>
  <build_depend>moveit_ros_planning_interface</build_depend>
  <build_depend>moveit_msgs</build_depend>

  <exec_depend>rclcpp</exec_depend>
  <exec_depend>moveit_ros_planning_interface</exec_depend>
  <exec_depend>moveit_msgs</exec_depend>

  <test_depend>ament_lint_auto</test_depend>
  <test_depend>ament_lint_common</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
